# Room Assets

这是 Live2D 小屋模式的资源目录。

当前使用的文件：

- `background.png`：小屋背景图
- `room_layout.json`：角色在不同状态下的位置、缩放和活动区域

路径规则：

- 仓库里的资源说明尽量只写相对路径
- 如果你从外部项目导出背景图，直接覆盖当前目录的 `background.png`
- 不要把你本机的绝对路径写进 README、配置模板或脚本

如果运行时出现棋盘格背景，通常说明图片不是真透明 PNG。优先重新导出带 alpha 的 PNG；临时兜底时再使用运行时配置去移除棋盘格。
